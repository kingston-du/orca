# Friend-first redesign repository inventory

Date: 2026-07-31
Observed commit: `c1ee45d0afd1b15810895543378d3bc54d004036` (`main`, matching `origin/main`)
Purpose: one-time evidence for the friend-first planning rebaseline. This file classifies the repository that exists before implementation of the redesigned product. It is not an implementation plan; `PROJECT.md` owns the target and sequence.

## Audit boundaries and evidence

The audit read the app routes and feature code, shared libraries, tests, all 14 migrations, all 11 pgTAP files, all Edge Functions and orchestration tests, scripts, generated database types, Expo/EAS/native configuration, CI, package manifests and lockfile, course notes, and recent Git history. `node_modules` was not treated as application source. No remote resource was changed.

At the observed commit:

- The only pre-existing untracked item was `RefactoringUI.pdf`; it is intentional and remains untracked.
- `.env` and Expo/Supabase local state are ignored and were not exposed or changed.
- The app audit found 19 Jest suites / 70 tests passing, along with TypeScript, lint, formatting, Expo dependency compatibility, and Expo Doctor 20/20.
- The existing database evidence records a clean 14-migration local replay, database lint/type agreement, 420 pgTAP assertions, real Data API/Storage/finalization/cleanup integration suites, and function tests. These results prove the Circle-era implementation at this commit, not the friend-first target.
- Repository evidence and link state show the first 10 migrations as promoted to the disposable hosted-development project and the last four posting migrations as local-only. A fresh authenticated remote inventory is mandatory before any rebaseline; this planning run did not mutate or freshly certify hosted state.

## Preserve unchanged

These pieces solve product-independent problems and should survive the rebaseline with only import or generated-type fallout where unavoidable.

| Area                                | Files                                                                                                                                           | Reason                                                                                                                                                                  |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Encrypted native session storage    | `src/lib/auth-storage.ts`                                                                                                                       | Keeps serialized Supabase sessions encrypted in AsyncStorage with a SecureStore-held key and fails closed on corruption/missing keys.                                   |
| Supabase client ownership           | `src/lib/supabase.ts`                                                                                                                           | Keeps client construction and lifecycle ownership isolated; retain the single refresh owner and synchronous Auth callback rules.                                        |
| TanStack Query foundation           | `src/lib/query-client.ts`, `src/lib/query-provider.tsx`, `src/lib/user-scoped-state.ts`, `src/lib/__tests__/query-provider.test.tsx`            | Remote state ownership, per-user isolation, and cleanup remain correct for the new domains.                                                                             |
| Auth domain core                    | `src/features/auth/auth-action-factory.ts`, `auth-actions.ts`, `auth-provider.tsx`, `auth-route-access.ts`, `auth-validation.ts`, focused tests | Open signup, verification, sign-in, recovery, sign-out, session restore, and route gating remain V1 requirements.                                                       |
| Auth route shells                   | `src/app/(auth)/*`                                                                                                                              | Thin routes remain the right Expo Router boundary; visual copy will be refined later without changing ownership.                                                        |
| Trusted JPEG parser                 | `supabase/functions/finalize-post/verify-jpeg.ts`, `supabase/functions/tests/verify-jpeg.test.mjs`                                              | The bounded parser validates decoded JPEG structure without trusting client MIME/extension. Rename-facing imports may change, but its verification behavior should not. |
| Toolchain/quality baseline          | `.nvmrc`, TypeScript/ESLint/Prettier/Jest configuration, compatible dependency pins                                                             | Strict TypeScript, deterministic installs, local database replay, test layers, and generated-type drift checks remain required.                                         |
| Auth email templates and empty seed | `supabase/templates/confirmation.html`, `supabase/templates/recovery.html`, `supabase/seed.sql`                                                 | Product-independent and still appropriate, subject to later brand/legal copy review.                                                                                    |

## Preserve but adapt

These contain reusable mechanics but currently encode Circle terminology, an incomplete Moment contract, or pre-beta placeholders.

| Area                       | Files                                                                                                                                                           | Required adaptation                                                                                                                                                                                                                                                                                            |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Root/provider routing      | `src/app/_layout.tsx`, `src/app/(app)/_layout.tsx`                                                                                                              | Retain provider and auth/onboarding gate ownership; point protected navigation at Home/Camera/People and the friend-first onboarding contract.                                                                                                                                                                 |
| Onboarding/legal           | `src/app/(app)/onboarding.tsx`, `src/features/onboarding/*`, migrations `20260727221157` and `20260727225836`, their tests                                      | Preserve server-validated 18+ and immutable version/hash acceptance. Add atomic canonical username claim; keep display name Unicode. Rebaseline into canonical migrations rather than amending promoted files.                                                                                                 |
| Account/profile foundation | migration `20260727041445`, `supabase/tests/account_foundation_test.sql`, generated profile callers                                                             | Preserve active/suspended/deleting fail-closed state. Replace profile visibility and shape with username, optional immutable/versioned avatar, friend/attribution rules, and narrow lookup RPCs.                                                                                                               |
| SQL security foundation    | migrations `20260726040517`, `20260726233832`, `supabase/tests/security_foundation_test.sql`                                                                    | Preserve extension/schema hardening, default privileges, and restricted auto-RLS machinery by folding reviewed behavior into the canonical baseline.                                                                                                                                                           |
| Open-signup correction     | migration `20260730044326`, `supabase/tests/open_account_signup_test.sql`                                                                                       | Preserve the final outcome—open signup and no signup hook—while omitting the obsolete gated-signup detour from the new baseline.                                                                                                                                                                               |
| Settings/account UI        | `src/app/(app)/(tabs)/settings.tsx`, `src/features/settings/*`                                                                                                  | Keep sign-out and user-scope cleanup behavior; move Settings behind My Profile and add profile, notifications, safety, legal/support, and deletion surfaces.                                                                                                                                                   |
| Camera/normalization       | `src/app/(app)/(tabs)/camera.tsx`, `src/features/posts/photo-capture-screen.tsx`, `photo-normalizer.ts`, their tests                                            | Preserve embedded camera lifecycle, front/rear, autofocus, orientation, mirrored selfie handling, Auto/Off flash, JPEG re-encode, 2048-pixel long edge and 6 MiB ceiling. Rename the feature from posts to Moments and add thumbnail/composer/draft ownership.                                                 |
| System picker              | `src/features/posts/photo-picker.ts`, its tests                                                                                                                 | Preserve single-image scoped system picker with no broad library permission. Replace device-now fallback: request the narrow EXIF payload, allowlist original time + explicit offset, discard all raw metadata/GPS, and classify missing/unreliable evidence as Archive.                                       |
| App/native/build config    | `app.config.js`, `app.json`, `eas.json`, Expo assets                                                                                                            | Preserve iOS-first development client and least-privilege intent. Replace Circle-era permission copy, make light-mode-first V1 deliberate, add universal-link/notification capabilities only at their checkpoints, and add preview/production profiles before beta.                                            |
| Supabase local config      | `supabase/config.toml`                                                                                                                                          | Preserve local Auth/OTP email, database, Storage, Studio/Inbucket, and function-emulation foundations. Adapt Data API exposure to `public` only, remove unused `graphql_public`, keep Realtime unused/disabled where supported, and add only settings earned by real checkpoints.                              |
| Posting security mechanics | local-only migrations `20260731005750`–`20260731044546`; `finalize-post`, `delete-post`, `reconcile-post-media`; `_shared/post-media-cleanup.ts`; scripts/tests | Reuse the reserve → exact non-upsert upload → trusted verify/finalize pattern, immutable object path, author/expired/orphan cleanup, leases, backoff, and no-SQL-Storage-delete rule. Rewrite every Circle key, policy, RPC, type, bucket, path, and test around friend-first Moments and snapshot recipients. |
| Verification scripts       | `scripts/test-post-media-storage.mjs`, `test-finalize-post.mjs`, `test-post-media-cleanup.mjs`                                                                  | Retain real HTTP/Data API/Storage testing style, fixture isolation, and lost-response assertions; replace Circle/post contracts and names.                                                                                                                                                                     |
| CI database job            | `.github/workflows/ci.yml`                                                                                                                                      | Retain clean replay → lint → pgTAP → real Storage/Function suites → type drift order; replace obsolete command targets only when the new suites land atomically.                                                                                                                                               |

## Temporarily retain

These must remain until an approved implementation checkpoint can replace them without leaving the repository broken.

| Item                                                                                 | Why it remains temporarily                                                                             | Removal/replacement gate                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| All 14 migration files and 11 pgTAP files                                            | They are the only reproducible schema/test history today; 10 also describe the old hosted-dev history. | Replace as one coherent local rebaseline after the founder approves Checkpoint 1A and the preflight/recovery evidence is captured.                                                                                        |
| `src/types/database.ts`                                                              | It accurately describes the current local 14-migration database and keeps current code type-safe.      | Regenerate only after the canonical friend-first migrations replay cleanly.                                                                                                                                               |
| Circle app/routes/tests and four-tab shell                                           | The checked-in app depends on them and is green.                                                       | Remove/replace atomically in Checkpoint 1A, never piecemeal while types/schema still disagree.                                                                                                                            |
| Local-only posting functions/tests/scripts                                           | They provide valuable secure-workflow reference and keep CI green until the old schema is replaced.    | Remove with the approved Checkpoint 1A rebaseline because they cannot remain green without Circle/post schema; preserve the standalone JPEG verifier and Git/course provenance, then build Moment equivalents in Phase 4. |
| Founder-only temporary legal text under `legal/` and `LICENSE` identity placeholders | External legal/business identity has not been approved. Hashes are internally consistent.              | Founder/legal review before any external beta. Do not invent company/support identity.                                                                                                                                    |
| Placeholder/default imagery under `assets/`                                          | Native builds currently reference it.                                                                  | Focused visual/brand checkpoint before beta; remove only after replacement manifests are verified.                                                                                                                        |
| `RefactoringUI.pdf`                                                                  | Intentional local visual reference, not product code.                                                  | Keep untracked unless the founder separately authorizes committing it.                                                                                                                                                    |

## Replace

These are not the target and should not shape new work.

| Area                         | Current files/objects                                                                | Replacement                                                                                                                 |
| ---------------------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- |
| Primary navigation           | `src/app/(app)/(tabs)/_layout.tsx`, Circle Home, Memories and tab Settings           | Home (Recent/Highlights), Camera, People; My Profile routes to Settings; conditional Past Shares under My Profile.          |
| Circle client domain         | `src/features/circles/*`, `src/app/(app)/circles/*`, Circle tests                    | Friend relationships, exact username lookup, invite links, requests, People, profiles, diaries, and Shared Moments.         |
| Circle database domain       | migrations `20260728233731`, `20260729000116`, `20260729064610`; Circle pgTAP suites | Canonical mutual friendship/request row, friend invite tokens, blocks, friend-first profiles and later Moment entitlements. |
| Signup-gate history          | migration `20260729072231` plus correction pair                                      | One canonical open-signup baseline with no Before User Created hook or signup-invite objects.                               |
| Circle-bound post schema     | all four local posting migrations and generated types                                | Friend-first `moments`, recipient snapshots, tags, reactions, seen state, trusted media evidence, and generic cleanup jobs. |
| Bucket/path vocabulary       | `post-media`, `{circle_id}/{author_id}/{post_id}/media.jpg`                          | Private `moment-media`, `{author_id}/{moment_id}/media.jpg`; private `avatars`; service-only `moderation-evidence`.         |
| Picker capture-time behavior | `exif: false` plus current-session/device-now fallback                               | Narrow allowlisted iOS original-capture metadata with explicit offset; missing/unreliable means Archive.                    |
| Product words                | Circle, Post, Memories tab, Everyone feed assumptions                                | Friends, user-facing Moment, Diary/Shared Moments, private recipient snapshots; no public/Everyone path.                    |

## Remove after approval

Removal is authorized in principle but must occur only inside the approved green rebaseline or the later replacement checkpoint named below.

- Delete `src/features/circles/` and `src/app/(app)/circles/` in Checkpoint 1A.
- Delete obsolete Circle tab routes (`memories.tsx`, old Circle `index.tsx`, tab `settings.tsx`) while adding the replacement routes in Checkpoint 1A; preserve Settings behavior in its new route.
- Remove Circle-specific migrations/tests and the obsolete signup-gate detour from the active migration history only under the approved rebaseline procedure.
- Remove `supabase/functions/delete-circle/`, `_shared/circle-cleanup.ts`, and `supabase/functions/tests/circle-cleanup.test.mjs` when the Circle schema is removed.
- Remove the local post finalization/cleanup function shells, scripts, and schema-bound tests in Checkpoint 1A after recording provenance; preserve/relocate `verify-jpeg.ts` and its test. Phase 4 rebuilds Moment equivalents from the archived design and Git history rather than leaving dead Circle code in a green branch.
- Remove genuinely unused Expo template assets and unused runtime dependencies only after an import/native-manifest audit at the focused polish checkpoint. `react-native-gesture-handler`, Reanimated, and Worklets are currently unused by app code, but no package change belongs in this planning run.

## Infrastructure inventory and gaps

Implemented infrastructure includes the private GitHub remote, main-branch CI, pinned Node/npm/Expo/Supabase toolchain, EAS development profile, encrypted native session persistence, local Supabase, handwritten migrations, generated types, pgTAP, real Storage/Data API integration tests, and a linked disposable hosted-development project. There is no production Supabase project and no external beta.

Not implemented: friend/username data model, People surfaces, Moment recipient/tag/reaction/seen model, feed/diary/shared queries, personal invite links, blocks/reports/moderation, notifications, account-deletion worker for the new graph, universal links, production/preview EAS profiles, production media backup, Sentry-equivalent crash/error monitoring, Maestro critical flows, or the friend-first app UI. The current Circle delete screen also calls its legacy RPC and does not prove completion through the local cleanup Edge Function; that mismatch is obsolete rather than a foundation to extend.

## Documentation inventory

| Document set                                | Classification and disposition                                                                                                                                                                                                                                                    |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PROJECT.md`                                | Replaced in this planning run with the exhaustive friend-first source of truth. It explicitly distinguishes target behavior from the still-Circle code.                                                                                                                           |
| `AGENTS.md`                                 | Replaced with sub-30-KiB autonomous checkpoint guidance; detailed product specification moved to `PROJECT.md`.                                                                                                                                                                    |
| `docs/course/README.md`, Lessons 1–3 and 11 | Preserve/adapt. The index now marks the redesign state; the retained lessons teach still-valid Auth/onboarding/session/camera foundations. Lesson 2 will gain username behavior and Lesson 11's downstream capture-time contract will be superseded by the implementation lesson. |
| Former Lessons 4–10 and 12–15               | Moved to `docs/course/archive/` and classified as conceptually reusable, superseded, unsafe/outdated, or historical. They retain Git provenance and do-not-implement warnings.                                                                                                    |
| Root `README.md`                            | Temporarily retain because this run's edit boundary does not include it. Its friend-groups/comment/Android and “guided build” wording is stale; Checkpoint 1A must rewrite it after the new code baseline exists.                                                                 |
| `legal/development-2026-07-27/*`, `LICENSE` | Temporarily retain founder-only development text and hash evidence. External legal/support/business identity and final retention language remain approval-gated.                                                                                                                  |
| `RefactoringUI.pdf`                         | Preserve as intentional untracked design input; do not commit without separate founder direction.                                                                                                                                                                                 |

## Rebaseline implication

The safe recommendation is a clean canonical friend-first migration history plus a fresh parallel hosted-development project. The current hosted project remains an untouched rollback/reference until the local baseline is green and a separately approved hosted promotion/cutover succeeds. Do not edit promoted migrations in place while continuing to use the old project, do not use migration-history repair to disguise incompatible history, and do not run `db reset --linked` against the old project as the default path. The exact workflow and approval gates live in `PROJECT.md`.
