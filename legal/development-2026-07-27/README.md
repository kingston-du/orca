# Orca development legal set

These documents are temporary development-only acceptance text for founder testing. They are not approved for external testers, production, or store submission.

Each database document hash is the lowercase SHA-256 digest of the corresponding Markdown file's exact UTF-8 bytes. Published replacements must use a new immutable version and require a new acceptance; never edit an already accepted version in place.
